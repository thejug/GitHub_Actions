name: Basic Conditional Example

on: push

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Run only on main
        if: github.ref == 'refs/heads/main'
        run: echo "This runs only on main branch"

      - name: Runs on success
        if: success()
        run: echo "Previous step(s) succeeded"
