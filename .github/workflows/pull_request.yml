name: PR Checks - pull_request

on:
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR code
        uses: actions/checkout@v4

      - name: Run tests
        run: |
          echo "Running tests on pull request code."
          cat hello-world.c
