name: PR Checks - pull_request_target

on:
  pull_request_target:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR code
        uses: actions/checkout@v4

      - name: Run tests
        run: |
          echo "Running tests on pull request code - from fix-branch"
          cat hello-world.c
