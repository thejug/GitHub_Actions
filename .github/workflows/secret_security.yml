name: SECRET Security Vulnerability

on:
  workflow_dispatch:

jobs:
  process:
    runs-on: ubuntu-latest

    steps:
      - name: Safe usage of SECRET
          run:|
            echo ${{ secrets.SECURITY_TEST }}
